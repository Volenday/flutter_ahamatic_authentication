import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// Web implementation of the FlutterAhamaticAuthentication plugin.
class FlutterAhamaticAuthenticationWeb {
  /// Registers the plugin with the Flutter web engine.
  static void registerWith(Registrar registrar) {
    // This plugin doesn't use method channels, so no registration needed.
    // All functionality is handled directly in Dart code.
  }
}

